name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: noclash
          POSTGRES_PASSWORD: noclash
          POSTGRES_DB: noclash
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U noclash -d noclash"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgres://noclash:noclash@localhost:5432/noclash
      TEST_DATABASE_URL: postgres://noclash:noclash@localhost:5432/noclash_test

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm typecheck

      - run: pnpm db:migrate

      - run: pnpm build

      - run: pnpm test
